type projectDetails = {
    name: string;
    date?: string;
    location?: string;
    role?: string;
    description?: string;
    class?: string;
}

let listOfProjects: projectDetails[] = [

];

export default listOfProjects;